name: Hello CI
on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from a fresh virtual machine"
      - run: whoami
      - run: pwd
      - run: ls -la