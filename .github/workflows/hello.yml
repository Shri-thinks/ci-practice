name: Hello CI
on: push

jobs:
  job-one:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "created by job one" > secret.txt
      - run: ls -la

  job-two:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ls -la
      - run: cat secret.txt

